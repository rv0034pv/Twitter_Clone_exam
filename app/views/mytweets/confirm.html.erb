<h3>以下の内容で、tweetする。</h3>
<%= form_with(model: @mytweet, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>tweet内容:<%= @mytweet.content %></p>
  <%= form.submit  "tweet!" %>
<% end %>
<%= link_to '戻る', :back %>
